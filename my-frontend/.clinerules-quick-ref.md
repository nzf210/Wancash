# Quick Reference - Development Rules

## 🚨 CRITICAL: Always Follow These Steps

### Before ANY Code Change:

#### 1️⃣ For Global Variable Changes

```bash
# Search for all usages
grep_search "variableName" "*.ts" "*.vue"

# Checklist:
☐ Found all consumers
☐ Checked Pinia stores
☐ Checked composables
☐ Checked components
☐ Verified reactive dependencies
```

#### 2️⃣ For API Endpoint Changes

```bash
# Search frontend
grep_search "/api/endpoint/path" "*.ts" "*.vue"

# Search backend
grep_search "endpoint/path" "*.ts"

# Checklist:
☐ Found all FE consumers
☐ Found BE route handler
☐ Verified request payload
☐ Verified response structure
☐ Checked error handling
☐ Updated TypeScript types
```

#### 3️⃣ Always Create Plan First

```markdown
1. Create implementation_plan.md
2. List all affected files
3. Document changes
4. Get user approval via notify_user
5. Then implement
```

---

## 📋 Quick Commands

### Search for variable usage:

```typescript
grep_search('variableName', ['*.ts', '*.vue'])
```

### Search for API endpoint:

```typescript
grep_search('/api/auth/verify', ['*.ts', '*.vue'])
```

### Find files by name:

```typescript
find_by_name('useAuth', 'src/')
```

---

## ⚡ Workflow Template

```
1. 🔍 SEARCH
   - Find all affected code
   - Document findings

2. 📋 PLAN
   - Create implementation_plan.md
   - List all changes
   - Identify risks

3. ✅ APPROVE
   - Use notify_user
   - Wait for user confirmation

4. 🛠️ IMPLEMENT
   - Make changes
   - Update all consumers

5. ✨ VERIFY
   - Test functionality
   - Create walkthrough.md
```

---

## 🎯 Key Files to Know

### Frontend

- `src/app/composables/useAuth.ts` - Authentication
- `src/app/router/index.ts` - Route guards
- `src/stores/` - Pinia stores
- `src/modules/` - Feature modules

### Backend

- `cloude-flare-wancash/src/auth.ts` - Auth endpoints
- `cloude-flare-wancash/src/index.ts` - Main router

---

## 💡 Remember

> **Never skip the planning phase!**
> **Always search before changing!**
> **Verify all consumers are updated!**
